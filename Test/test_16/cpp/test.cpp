// Thêm các thư viện cần thiết
#include <iostream>
    
// Khởi tạo kiểu dữ liệu
class Node {
    // Dữ liệu
    int data;

    // Nút con
    Node* left;
    Node* right;

    // Khai báo dữ liệu
    Node(int val) : data(val), left(nullptr), right(nullptr) {};
};

int main() {

    return 0;
}
